<%= form_with(model: woman, local: true) do |form| %>
  <% if woman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(woman.errors.count, "error") %> prohibited this woman from being saved:</h2>

      <ul>
      <% woman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
